name: CI/CD Pipeline for MediTrack
on:
    pull_request:
        branches:
            - main

jobs:
    build:
        runs-on: ubuntu-24.10
        

        steps:
            - name: Testing 
              run: echo "Hello world"

            
            - name: Pass the job
              run: exit 0